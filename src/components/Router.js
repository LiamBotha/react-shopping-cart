import { BrowserRouter } from "react-router-dom";

function Router() {
    
    return <div>
        <BrowserRouter>
            <switch>
            </switch>
        </BrowserRouter>
    </div>
}

export default Router;